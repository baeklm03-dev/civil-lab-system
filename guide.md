# คู่มือระบบทดสอบวัสดุ — ภาควิชาวิศวกรรมโยธา มจพ. พระนครเหนือ

เอกสารนี้สรุปสิ่งที่ระบบทำได้ในปัจจุบัน (2026-07-16) ไว้เป็นจุดอ้างอิงเดียว

## ภาพรวมสถาปัตยกรรม

- **Frontend**: React 18 + Vite + Tailwind CSS, routing ด้วย React Router
- **Backend**: Node.js + Express, ยืนยันตัวตนด้วย JWT
- **ที่เก็บข้อมูล**: ทำงานได้ 2 โหมดคู่ขนานในทุกโมดูล
  - ถ้าตั้งค่า `*_SHEET_ID` ที่เกี่ยวข้องไว้ใน `.env` → อ่าน/เขียนผ่าน Google Sheets API
  - ถ้าไม่ตั้งค่า → อ่าน/เขียนไฟล์ JSON ในเครื่อง (`backend/data/*.json`)
- **PDF**: สร้างผ่าน Puppeteer (headless Chrome) ฝั่ง backend จาก HTML template
- **Google Sheet ใบรับงาน**: อัปโหลดไฟล์ template จริง (`.xlsx`) ขึ้น Google Drive แล้วแปลงเป็น Sheet ในตัว (รักษาสูตร/dropdown/รูปแบบจากไฟล์ต้นฉบับ) แล้วเติมเฉพาะค่าที่เปลี่ยนต่อใบงาน

## บทบาทผู้ใช้งาน (role)

| role | สิทธิ์ |
|---|---|
| `user` | ใช้งานทั่วไป: สร้าง/ดูใบงาน, บันทึกผลทดสอบ, ออกเอกสาร |
| `admin` | เหมือน user + จัดการบุคลากร, จัดการข้อความแจ้ง |
| `superadmin` | เหมือน admin + จัดการผู้ใช้งาน, ดูประวัติการใช้งานระบบ (activity log) |

> **หมายเหตุ**: ปัจจุบัน endpoint สร้าง/แก้ไขใบงานทดสอบ (`workorders.js`) และบันทึกผลทดสอบ (`results.js`) เช็คแค่ "ล็อกอินแล้วหรือยัง" ไม่ได้บังคับ role — เป็นการตัดสินใจที่ตั้งใจพักไว้ก่อนตามที่ผู้ใช้ระบบแจ้งไว้ตอนเริ่มโปรเจกต์

## หน้าเว็บ (Frontend Pages)

| หน้า | เส้นทาง | หน้าที่ |
|---|---|---|
| เข้าสู่ระบบ | `/login` | ล็อกอินด้วย username/password |
| ภาพรวมระบบ | `/dashboard` | สรุปสถิติ, กราฟ, ตัวกรองช่วงวันที่, นำออกเอกสาร (Export) |
| ใบงานทดสอบ | `/workorders` | รายการใบงานทั้งหมด, ค้นหา/กรองสถานะ, สร้างใบงานใหม่, นำเข้าใบงาน (import) |
| รายละเอียดใบงาน | `/workorders/:refNo` | ข้อมูลใบงาน, อัปเดตสถานะ, ลิงก์ Google Sheet, ปุ่ม "ออกเอกสาร" |
| บันทึกผลการทดสอบ | `/workorders/:refNo/record-results` | กรอกผลทดสอบดิบ (คอนกรีต/เหล็กเส้น/อื่นๆ), วางจาก Excel ได้, บันทึก + สร้าง PDF ได้ทันที |
| แบบฟอร์มสำหรับบันทึกผล | `/workorders/:refNo/print-form` | แบบฟอร์มเปล่า A4 ให้ช่างบันทึกผลด้วยลายมือ, ส่งออกเป็น PDF จริง |
| จัดการบุคลากร | `/personnel` | รายชื่ออาจารย์/เจ้าหน้าที่ทดสอบ |
| จัดการผู้ใช้งาน (superadmin) | `/admin/users` | สร้าง/แก้ไข/ปิดใช้งาน/รีเซ็ตรหัสผ่าน ผู้ใช้ระบบ |
| ประวัติการใช้งาน (superadmin) | `/admin/logs` | activity log ของการกระทำสำคัญในระบบ, export |
| จัดการข้อความแจ้ง (admin+) | `/admin/announcements` | เพิ่ม/แก้ไข/ปิดการแจ้งเตือนที่จะไปโผล่ในใบงาน |

## ฟีเจอร์หลัก

### 1. ใบงานทดสอบ (Work Orders)
- สร้างใบงานใหม่พร้อมออกเลขที่อ้างอิง (REF NO.) อัตโนมัติ
- แยก **ประเภทการทดสอบ** (คอนกรีต / เหล็กเส้น / อื่นๆ กำหนดคอลัมน์เอง) ออกจาก **ประเภทตัวอย่าง** (Cube/Cylinder/Coring — แสดงเฉพาะตอนเลือกคอนกรีต, ไม่บังคับกรอก)
- นำเข้าใบงานหลายรายการพร้อมกันผ่านการวางข้อมูล (paste จาก Excel)
- เลือกข้อความแจ้ง/ประกาศแบบ multi-select แนบไปกับใบงานได้
- รายการใบงานเรียงจากล่าสุด → เก่าสุด, 15 รายการต่อหน้า
- อัปเดตสถานะใบงาน: รับเรื่อง → รอข้อมูล → ดำเนินการ → เสร็จสิ้น
- สรุปยอดรายรับ (finance summary) ต่อใบงานจากราคาต่อรายการเหล็กเส้น

### 2. เชื่อมต่อ Google Sheet
- เมื่อสร้างใบงาน ระบบจะอัปโหลด template จริง (`Concrete Test Form-sheet.xlsx` / `Steel Test Form-sheet.xlsx`) ขึ้น Google Drive แปลงเป็น Sheet อัตโนมัติ พร้อม dropdown ที่ดึงจากชีทรายการราคาในตัว
- เติมค่าที่เปลี่ยนต่อใบงาน (ข้อมูลลูกค้า, รายการเหล็กเส้น) ลง cell ที่ตรงตำแหน่งจริงของ template
- เขียนข้อความแจ้ง/ประกาศที่เลือกไว้ลง `L14:L22` ของ Sheet อัตโนมัติ
- ซิงก์ข้อมูลที่ลูกค้ากรอกกลับจาก Sheet เข้าระบบได้ (sync-sheet)

### 3. บันทึกผลการทดสอบ
- บันทึกผลดิบแยกตามชนิดการทดสอบ:
  - **คอนกรีต**: วันที่หล่อ, มิติ 1-3, น้ำหนัก, แรงกดสูงสุด → คำนวณพื้นที่/ปริมาตร/ความหนาแน่น/หน่วยแรงอัตโนมัติ (สูตรต่างกันตามทรงกระบอก/ลูกบาศก์)
  - **เหล็กเส้น**: ขนาดเหล็กเส้นระบุ, ความยาว, น้ำหนัก, แรงคราก/แรงดึงสูงสุด, ระยะยืด, ระยะเกจ → คำนวณพื้นที่หน้าตัดระบุ/หน่วยแรงครากและดึงสูงสุดอัตโนมัติจากสูตรเรขาคณิต
  - **อื่นๆ**: กำหนดคอลัมน์เอง
- เพิ่มแถวได้ไม่จำกัด, วางข้อมูลจาก Excel ได้
- กดบันทึกแล้วสร้าง PDF รายงานผลได้ทันทีในหน้าเดียว

### 4. ออกเอกสาร (ต่อใบงาน)
เมนู "ออกเอกสาร" ในหน้ารายละเอียดใบงานมี 3 ตัวเลือก:
1. **แบบฟอร์มสำหรับบันทึกผล** — ฟอร์มเปล่า A4 ให้ช่างจดด้วยลายมือ (มีเช็คบ็อกซ์ชนิดตัวอย่าง, ปรับหัวข้อ/คอลัมน์เองได้ถ้าเป็นการทดสอบแบบอื่นๆ), ส่งออก PDF จริง ไม่ต้องเปิด print dialog
2. **บันทึกผลการทดสอบ** — กรอกผลดิบแบบดิจิทัล พร้อมสร้าง PDF ได้ทันที
3. **ใบรายงานผลการทดสอบ** — รายงานผลอย่างเป็นทางการ ดึงข้อมูลที่บันทึกไว้จาก "บันทึกผลการทดสอบ" มาเติมอัตโนมัติ (auto-detect ว่าเป็น Tension หรือ Compression Test)

### 5. ภาพรวมระบบ (Dashboard)
- สถิติภาพรวม: จำนวนใบงานตามสถานะ, ตามประเภทตัวอย่าง, รายรับรวม
- กราฟแนวโน้ม
- กรองข้อมูลตามช่วงวันที่
- นำออกเอกสาร (Export) เป็นไฟล์ตามช่วงวันที่ที่กำหนด

### 6. จัดการผู้ใช้งาน/ประวัติการใช้งาน (superadmin)
- CRUD ผู้ใช้งานระบบ, เปิด/ปิดการใช้งาน, รีเซ็ตรหัสผ่าน
- Activity log บันทึกการกระทำสำคัญ (สร้างใบงาน, สร้าง PDF, ล็อกอิน ฯลฯ) พร้อม export

### 7. ข้อความแจ้ง/ประกาศ (admin+)
- CRUD ข้อความประกาศ, เปิด/ปิดการใช้งาน
- เลือกแนบแบบ multi-select ตอนสร้างใบงาน → ไปโผล่ในพื้นที่ประกาศของ Google Sheet โดยอัตโนมัติ

## API Endpoints (backend)

| Route | Method + Path |
|---|---|
| Auth | `POST /login`, `GET /me`, `POST /logout` |
| Work Orders | `GET /stats`, `GET /`, `GET /:refNo`, `GET /:refNo/finance-summary`, `POST /`, `POST /import`, `PATCH /:refNo/status`, `PATCH /:refNo/custom-columns`, `PATCH /:refNo/result`, `POST /:refNo/sync-sheet`, `POST /:refNo/create-sheet` |
| Results | `GET /`, `POST /`, `PUT /:id`, `DELETE /:id` |
| Personnel | `GET /`, `POST /`, `PUT /:id`, `PATCH /:id/status`, `DELETE /:id` |
| Announcements | `GET /`, `POST /`, `PUT /:id`, `PATCH /:id/status`, `DELETE /:id` |
| Admin Users | `GET /`, `POST /`, `PUT /:id`, `PATCH /:id/status`, `DELETE /:id`, `POST /:id/reset-password` |
| Admin Logs | `GET /`, `GET /export` |
| Reports | `POST /generate` (PDF จาก template จริง), `POST /export-html` (PDF จาก HTML ดิบ) |
| Export | `GET /dashboard`, `GET /` |

## การตั้งค่า (.env)

| ตัวแปร | ใช้ทำอะไร |
|---|---|
| `PORT`, `NODE_ENV`, `FRONTEND_URL` | ตั้งค่าเซิร์ฟเวอร์ทั่วไป |
| `JWT_SECRET`, `JWT_EXPIRES_IN` | เข้ารหัส token |
| `LOCAL_ADMIN_*` | บัญชี admin สำรอง (โหมด local, ไม่ผ่าน Sheet) |
| `GOOGLE_SERVICE_ACCOUNT_EMAIL`, `GOOGLE_PRIVATE_KEY`, `GOOGLE_PROJECT_ID` | เข้าถึง Google Sheets/Drive แบบ service account เท่านั้น (ไม่มี OAuth user flow แล้ว) — ต้องแชร์สิทธิ์ Sheet/Drive folder ที่เกี่ยวข้องทุกอันให้อีเมลนี้ก่อนใช้งาน |
| `DRIVE_FOLDER_ID` | โฟลเดอร์ปลายทางที่จะสร้าง Sheet ใบรับงาน |
| `OWNER_EMAIL` | อีเมลที่จะแชร์สิทธิ์แก้ไข Sheet ที่สร้างใหม่ให้อัตโนมัติ |
| `SPREADSHEET_ID`, `WORKORDERS_SHEET_ID`, `USERS_SHEET_ID`, `PERSONNEL_SHEET_ID` | ถ้าตั้งค่า จะสลับโมดูลนั้นไปใช้ Google Sheets แทนไฟล์ JSON ในเครื่อง |

## จุดที่ยังไม่ได้ทำ / ตั้งใจพักไว้

- ตารางรายการต่อแถวของคอนกรีต (วันที่หล่อ/ชนิดตัวอย่าง/กำลังคาดหวัง) ใน Google Sheet ปล่อยว่างให้ลูกค้ากรอกเอง ยังไม่มี field คู่กันในระบบ

## สิทธิ์ตาม role

สร้าง/แก้ไขใบงานและบันทึกผลทดสอบเปิดให้ทุก role ที่ล็อกอินแล้ว (เป็นงานหลักประจำวันของ staff) — ยกเว้น
**ลบผลทดสอบ** (`DELETE /api/results/:id`) ที่จำกัดไว้แค่ `admin`/`superadmin` เพราะเป็นการทำลายข้อมูลถาวร กู้คืนไม่ได้
